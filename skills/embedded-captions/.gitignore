# Regenerable font SOURCES (latin-subset woff2 from @fontsource/jsdelivr).
# Inference reads the base64-inlined assets/fonts/fonts.css, NOT these — they are
# build-only inputs for assets/fonts/build-fonts-css.cjs (re-fetch to rebuild fonts.css).
assets/fonts/files/
